# Pages'e gitmemeli — deneme/kaynak/ara dosyalar (feed yalnız cover*.jpg + episodes/*.mp3 + *.json + feed.xml kullanır)
cover-options/
*.png
*.wav
*.dur

# Ses dosyalari artik Cloudflare R2den servis ediliyor (cdn.noronmedia.com).
# Yerelde dururlar (yedek + feed boyut hesabi) ama Pages/gite GIRMEZLER.
*.mp3
